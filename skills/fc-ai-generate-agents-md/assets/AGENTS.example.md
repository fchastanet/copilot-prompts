# AGENTS.md

## 1. Project

One-sentence description. Link to README or Confluence for background.

## 2. Environment Setup

- {Main programming language(s) and framework(s) with versions}
- {install command}
- {start dev server command}
- {Install dependencies command}
- Configure AWS: `aws configure` (requires credentials)

### 2.1. Secret Storage Locations

- **Production**: AWS Secrets Manager (`prod/*` namespace)
- **Staging**: AWS Secrets Manager (`staging/*` namespace)
- **Development**: Local `.env` file (gitignored, copy from `.env.example`)
- **CI/CD**: GitHub Actions Secrets

### 2.2. Required Environment Variables (Structure Only)

- `DATABASE_URL`: PostgreSQL connection (format: postgresql://user:pass@host:port/db)
- `API_KEY`: External API auth (retrieve from secrets manager)
- `JWT_SECRET`: Token signing (minimum 32 characters, from secrets manager)

## 3. Technology Stack

- {List the main technologies used in the codebase, e.g. "PHP 8.2 with Symfony 6.4, MySQL 8, Redis, AWS S3, ..."}

## 4. Build & Test

- {run all tests}
- {run linter/static analysis}
- {run a single test file}

## 5. Conventions

- {Naming: just name the generic convention used if any (PSR-12, Symfony, ...) or describe naming conventions for files,
  functions, variables, ... in file .github/instructions/CONVENTIONS.md and link to it here}
- {language version, framework version}
- {Link to the skill that defines coding conventions}
- {branch/commit naming rule if not standard}
- {Error handling: describe the project approaches to errors and exceptions}

## 6. Structure

- {describe how the codebase is organized, not too detailed, just the main directories and their purpose. Do not list
  every file or class.}

## 7. Key directories

- {List the main directories in the codebase and what they contain. Restrict to level 1 or 2 directories.}

- `classes/` - main code

- `source/` - legacy code, no namespaces, direct SQL queries

- ...

## 8. Monorepo Instructions (if applicable)

{Cross-package dependencies}

## 9. Workflow

- Describe PR conventions, branch naming, and commit style
  - Eg: Title format: `[fix/PROJECT-123][component] Brief description`
- Reference specific instruction files for detailed standards:
  - Language guidelines: `.github/instructions/{language}.instructions.md`
  - Testing: `.github/instructions/testing.instructions.md`
    - Eg: "Tests required for new features, use PHPUnit for PHP code, and Jest for JS code. Test files should mirror the
      structure of source files under `tests/`."
  - Security: `.github/instructions/security.instructions.md`
  - Documentation: `.github/instructions/documentation.instructions.md`
  - Performance: `.github/instructions/performance.instructions.md`
  - Code review: `.github/skills/code-review/SKILL.md`

## 10. Glossary

<!-- Add domain abbreviations or non-obvious mappings found in the code. -->

- **Term**: Meaning
- ...

## 11. ⚠ Questions for reviewer

<!-- Remove this section once answered. These are things AI cannot deduce. -->

- [ ] Q: …

## 12. Points of attention

<!-- Add any non standard patterns, risky areas, or things you noticed in the code that would be important for an AI to know when generating code in this directory. -->

- {e.g. "This directory contains code automatically generated by X tool, so it should not be edited directly."}
- {e.g. "This directory is legacy code that is no longer maintained, so new code should not be added here."}
- ...

### 12.1. Security Rules

- NEVER commit `.env` files
- NEVER hardcode credentials
- NEVER log secret values
- NEVER pass secrets as command-line arguments
- ALWAYS use secrets manager for production
